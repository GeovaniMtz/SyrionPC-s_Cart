spring.application.name=cart

# Puerto del cart
server.port=8082

# Config Service
spring.config.import=optional:configserver:
spring.cloud.config.uri=http://localhost:8888

# Registry Service
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# Admin Service
# Exponer endpoints actuator
management.endpoints.web.exposure.include=health,info,metrics,loggers,threaddump,heapdump
# Mostrar detalles completos en /actuator/health
management.endpoint.health.show-details=always
# Configurar cómo se registra en Eureka
eureka.instance.hostname=localhost
eureka.instance.prefer-ip-address=false

#BD
spring.datasource.url=jdbc:mysql://localhost:3306/DB_Syrion
spring.datasource.username=root
#spring.datasource.password=Root12345!
spring.datasource.password=GaSQL.
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# Swagger/OpenAPI
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.doc-expansion=none

#Url del product
product.service.url=http://localhost:8080
